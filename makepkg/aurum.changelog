## 0.2

* add `-w`
  + only prompt (`view-delta`) when building packages
* add `-C` / `--chdir`
* implement `-F` / `--fetch`
  + add `--merge`, `--rebase`, `--reset`, `--ff`

## 0.1

* initial release
